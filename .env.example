# Kernel / Marketplace upstreams (optional; defaults to local stubs)
KERNEL_URL=http://localhost:4000
MARKETPLACE_URL=http://localhost:4100

# StorySphere backend upstream (optional; defaults to local stubs)
STORYSPHERE_BACKEND_URL=http://localhost:4200
