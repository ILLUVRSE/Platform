terraform {
  backend "s3" {
    bucket         = "illuvrse-terraform-state"
    key            = "staging/terraform.tfstate"
    region         = "us-east-1"
    dynamodb_table = "illuvrse-terraform-locks"
    encrypt        = true
  }
}
